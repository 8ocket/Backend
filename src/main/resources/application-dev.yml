spring:
  datasource:
    url: jdbc:postgresql://${DB_DEV_HOST}:${DB_DEV_PORT}/${DB_DEV_DATABASE}
    username: ${DB_DEV_USERNAME}
    password: ${DB_DEV_PASSWORD}

  data:
    redis:
      host: localhost
      port: 6379

kakao:
  redirect-uri: ${KAKAO_LOCAL_REDIRECT_URI}

google:
  redirect-uri: ${GOOGLE_LOCAL_REDIRECT_URI}

session:
  baseurl: ${SESSION_LOCAL_BASE_URL}